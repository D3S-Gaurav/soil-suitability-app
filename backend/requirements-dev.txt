# Development / test dependencies.
# Install on top of the runtime deps:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0.0
# Required by fastapi.testclient.TestClient (Starlette's test transport).
httpx>=0.27.0
